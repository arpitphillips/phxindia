{
  "name": "phxindia-site",
  "version": "1.0.0",
  "private": true,
  "description": "Rebuilt static website for PHX India (Photographix) — see README.md",
  "scripts": {
    "build:ts": "tsc -p tsconfig.json",
    "build": "bash scripts/build.sh",
    "serve": "python3 scripts/serve.py"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
